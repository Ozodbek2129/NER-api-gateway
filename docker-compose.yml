services:
  minio:
    container_name: minio
    image: quay.io/minio/minio:latest  # ✅ latest ishlatish
    command: server /data --console-address ":9001"
    ports:
      - '9000:9000'
      - '9001:9001'
    volumes:
      - 'minio_data:/data'
    environment:
      - MINIO_ROOT_USER=minio
      - MINIO_ROOT_PASSWORD=minioadmin
      - MINIO_DEFAULT_BUCKETS=contracts
    healthcheck:
      test: [ "CMD-SHELL", "curl -f http://192.168.0.44:9000/minio/health/live || exit 1" ]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - ner

networks:
  ner:

volumes:
  minio_data: